# yaml-language-server: $schema=../tools/rule.schema.json
# dplaax rule catalog — resolver (VC resolver: hash-keyed content-addressed store)
# File split is non-normative. Entry identity is carried by id (→ README.md).
rules:
  - id: resolver.address.form
    status: draft
    class: core
    statement: >-
      The key for a VC resolver is a content hash of the form
      sha256:<lowercase-hex>, computed over the canonicalized byte sequence of
      the credential body excluding the proof, and MUST use the same
      canonicalization as chain links (previousCredential).
    uses: [canon.jcs.base, credential.prev.content-hash]
    schemas: []
    vectors: [resolver-001, resolver-002]
    notes: >-
      The key point is that the resolver key equals the chain-link hash that
      the verifier recomputes from the received credential — if the same
      canonicalization is not shared, resolution fails.
      Source: drafts/v1_002 vc_resolver section (Address form).

  - id: resolver.immutability
    status: draft
    class: core
    statement: >-
      The same key MUST resolve to the same signed credential document. A
      verifier MUST recompute the key content hash from the returned document
      (proof excluded; resolver.address.form) and MUST reject any response
      whose recomputed hash does not match the key.
    uses: [resolver.address.form]
    schemas: []
    vectors: [resolver-003]
    notes: >-
      Immutability is cryptographically enforced — the resolver operator is
      not a trust principal and cannot forge content (availability is the only
      operator discretion). Returning a different document via expiration /
      migration / operator override also violates immutability. The
      resolver-side obligation is document-level: the same content signed
      with a different proof is a different document, and swapping it under
      an already-served key breaks source_root recomputation agreement
      (commitment.source-root.leaf hashes the proof-included document) even
      though the content hash still matches. The verifier-side check is the
      strongest stateless one: parse the returned document, strip the proof,
      canonicalize, hash, compare to the key — content substitution is always
      caught; proof substitution is not statelessly detectable but can only
      break recomputation closed (a mismatching root can never turn into a
      false verified — that would need a second-preimage). Byte-level jitter
      that canonicalizes to the identical document carries no authority.
      Revised 2026-07-02 (full-review A-2 consequence): the earlier "hash of
      the returned bytes" wording assumed a proof-excluded body and would
      have a conformant verifier reject every proof-bearing response.
      Source: drafts/v1_002 vc_resolver section (Immutability invariant /
      Trust model).

  - id: resolver.states
    status: draft
    class: core
    statement: >-
      A lookup MUST yield Resolved, Unavailable, or NotFound. NotFound is a
      definitive report of not holding the key. A verifier MUST map
      Unavailable to indeterminate, NotFound from a non-existence authority
      to failed, and NotFound otherwise to indeterminate.
    uses: [confidence.axes.three-state]
    schemas: []
    vectors: [resolver-004, resolver-005, resolver-009]
    notes: >-
      Definitions: Unavailable means the lookup could not establish whether
      the source holds the key, including transient inability to answer;
      NotFound is the source's definitive report of not holding it. The
      states are source-local observations, and their evidentiary weight is
      a separate question of authority: a NotFound proves global
      non-existence only when the source is authoritative for non-existence
      in the queried identifier's namespace — a content-addressed VC store
      is not such an authority. The Unavailable/NotFound distinction
      underpins the determinism of confidence-axis evaluation — it separates
      what may be resolved on retry from what cannot. Revised 2026-07-13 (P0-11 / L6 reconciliation): the earlier
      unscoped "NotFound to failed" let any third-party store mint a
      definitive failed for content it never held (an audit-DoS lever) and
      contradicted the commitment family's pinned semantics (commitment-009:
      unresolved sources map to indeterminate). Authority scoping closes both
      directions fail-closed — absence of data never yields a definitive
      negative, and hiding data never yields verified. In dplaax no
      VC-content non-existence authority exists: content hashes are
      self-certifying, and definitive emission/absence claims are the
      transparency log's concern, not the resolver's. The DID registry is the
      naming authority for its DIDs, so DID resolution keeps the failed
      mapping (resolver-005); a content store's NotFound is a retry-routing
      signal and maps to indeterminate (resolver-009).
      Source: drafts/v1_002 vc_resolver section (Lookup interface).

  - id: resolver.states.no-demotion
    status: draft
    class: core
    statement: >-
      A resolver MUST NOT transition a hash that has previously returned
      Resolved to NotFound. If eviction is implemented, an evicted hash MUST
      transition to Unavailable.
    uses: [resolver.states, resolver.immutability]
    schemas: []
    vectors: [resolver-006]
    notes: >-
      A body that was once observed as resolvable is part of verifiable
      history: audits recompute over previously served documents, so a store
      that forgets what it served breaks audit reproducibility. That
      durable-history obligation, not the evidentiary weight of NotFound, is
      what grounds the prohibition (revised 2026-07-13, P0-11 — under the
      authority-scoped resolver.states a content store's NotFound is a
      store-local report, no longer a global non-existence claim, but
      demoting a previously Resolved key still destroys history the audit
      path relies on). Permanent deletion (e.g. for regulatory compliance)
      is a documented out-of-protocol matter; the protocol provides no
      removed state.
      Source: drafts/v1_002 vc_resolver section (Pool semantics).

  - id: resolver.batch.shape
    status: draft
    class: core
    statement: >-
      If an implementation or profile provides batch lookup, a batch
      response MUST have the same entry count and order as the request; each
      entry state is independent; partial availability is a non-error state;
      bulk fetch MUST preserve per-hash state semantics.
    uses: [resolver.states]
    schemas: []
    vectors: [resolver-007]
    notes: >-
      Partial availability is the natural state of a distributed resolver
      (peer cache / CDN); surfacing it as an error would break the
      indeterminate semantics of confidence evaluation.
      Reserved rule (recorded 2026-07-13, P0-11): the dplaax.vc.v1 wire
      profile defines no client-facing batch lookup RPC — the reference
      implementation's batch resolver is an internal async worker, not a
      lookup surface. The rule binds any batch surface an implementation or
      profile adds, without obligating one to exist; resolver-007 pins the
      shape such a surface must satisfy.
      Source: drafts/v1_002 vc_resolver section (Batch lookup / Partial
      availability).

  - id: resolver.body.encoding
    status: draft
    class: core
    statement: >-
      A response body is the canonical JSON byte sequence of the signed
      credential document (body and proof); the dplaax wire default encoding
      is base64url (no padding). A wire profile MAY substitute a transport
      encoding but MUST preserve semantic equivalence.
    uses: [resolver.address.form]
    schemas: []
    vectors: [resolver-008]
    notes: >-
      The body carries the signed document because the audit path needs the
      proof: proof re-verification and source-root leaf recomputation
      (commitment.source-root.leaf) are computed over the proof-included
      canonical form. The resolver key is unchanged — it stays the
      proof-excluded content hash of resolver.address.form — so key
      verification is the parse-strip-recompute of resolver.immutability,
      not a byte hash of the body. Revised 2026-07-02 (full-review A-2
      consequence): the earlier "excluding the proof" text predated the
      2026-06-11 source-commitment rework and contradicted the audit path;
      the implementation serves the signed document.
      Source: drafts/v1_002 vc_resolver section (Partial availability
      encoding).
